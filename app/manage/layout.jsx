export const metadata = {
    title: "Manage",
    description: "Manage",
  };

export default function ManageLayout({
    children,
  }) {
    return (
      <section>
        {children}
      </section>
    )
  }