export const metadata = {
    title: "Getm",
    description: "Getm",
  };

export default function GetmLayout({
    children,
  }) {
    return (
      <section>
        {children}
      </section>
    )
  }