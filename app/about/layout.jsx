export const metadata = {
    title: "filldata",
    description: "filldata",
  };

export default function AboutLayout({
    children,
  }) {
    return (
      <section>
        {children}
      </section>
    )
  }