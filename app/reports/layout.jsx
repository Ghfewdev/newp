export const metadata = {
    title: "Report",
    description: "Report",
  };

export default function ReportLayout({
    children,
  }) {
    return (
      <section>
        {children}
      </section>
    )
  }